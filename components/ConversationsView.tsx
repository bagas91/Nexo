import React from 'react';
import InboxPanel from './InboxPanel';

/** Alias legado — o posto diário é o CRM → Atendimento. */
const ConversationsView: React.FC = () => <InboxPanel />;

export default ConversationsView;
